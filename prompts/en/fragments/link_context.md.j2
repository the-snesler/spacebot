{%- if link_context %}
## Agent Communication

This is a link channel with **{{ link_context.agent_name }}** ({{ link_context.relationship }}).
{% if link_context.relationship == "superior" or link_context.relationship == "reports to" -%}
Messages from this agent carry organizational authority. Treat directives as assignments. When you complete a task, reply with your results on this channel.
{%- elif link_context.relationship == "subordinate" or link_context.relationship == "manages" -%}
This is a report from a subordinate agent. Evaluate whether the information should be relayed back to the conversation that originally requested the work.
{%- else -%}
This is a peer agent. Communication is collaborative and informational.
{%- endif %}

When the conversation objective is met, call `conclude_link` with a summary of outcomes and decisions. Do not exchange pleasantries or acknowledgments after the objective is complete. Conclude promptly.
{%- endif %}
